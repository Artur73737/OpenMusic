# Music MCP v2 — Backend dependencies
# Install: pip install -r requirements.txt

# ── Core framework ──
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── HTTP client ──
httpx>=0.24.0
certifi>=2023.0.0

# ── Logging ──
structlog>=23.0.0

# ── Utilities ──
python-dotenv>=1.0.0
python-multipart>=0.0.6

# ── SSE ──
sse-starlette>=1.0.0

# ── Database ──
SQLAlchemy>=2.0.0
aiosqlite>=0.19.0

# ── Auth ──
PyJWT>=2.0.0

# ── Dev tools ──
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
pytest>=7.0.0
pytest-asyncio>=0.20.0
pytest-cov>=4.0.0
